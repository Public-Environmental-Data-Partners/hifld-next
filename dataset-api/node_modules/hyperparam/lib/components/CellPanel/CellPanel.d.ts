import type { DataFrame } from 'hightable';
interface ViewerProps {
    df: DataFrame;
    row: number;
    col: number;
    setProgress: (progress: number) => void;
    setError: (error: unknown) => void;
    onClose: () => void;
}
/**
 * Cell viewer displays a single cell from a table.
 */
export default function CellPanel({ df, row, col, setProgress, setError, onClose }: ViewerProps): import("react/jsx-runtime").JSX.Element;
export {};
