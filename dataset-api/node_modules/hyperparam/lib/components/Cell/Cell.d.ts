import type { FileSource } from '../../lib/sources/types.js';
interface CellProps {
    source: FileSource;
    row: number;
    col: number;
}
/**
 * Cell viewer displays a single cell from a table.
 */
export default function CellView({ source, row, col }: CellProps): import("react/jsx-runtime").JSX.Element;
export {};
