import { ReactNode } from 'react';
export interface ContentSize {
    fileSize?: number;
}
export interface TextContent extends ContentSize {
    text: string;
}
interface ContentWrapperProps {
    content?: ContentSize;
    headers?: ReactNode;
    isLoading?: boolean;
    children?: ReactNode;
}
export default function ContentWrapper({ content, headers, isLoading, children }: ContentWrapperProps): import("react/jsx-runtime").JSX.Element;
export {};
