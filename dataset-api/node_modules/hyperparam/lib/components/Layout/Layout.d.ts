import { ReactNode } from 'react';
interface LayoutProps {
    children: ReactNode;
    progress?: number;
    error?: Error;
    title?: string;
}
/**
 * Layout for shared UI.
 * Content div style can be overridden by className prop.
 *
 * @param props
 * @param props.children - content to display inside the layout
 * @param props.progress - progress bar value
 * @param props.error - error message to display
 * @param props.title - page title
 */
export default function Layout({ children, progress, error, title }: LayoutProps): import("react/jsx-runtime").JSX.Element;
export {};
