import type { DirSource, FileSource } from './types.js';
type RepoType = 'model' | 'dataset' | 'space';
interface BaseUrl {
    source: string;
    origin: string;
    type: RepoType;
    repo: string;
    branch: string;
    path: string;
}
interface DirectoryUrl extends BaseUrl {
    kind: 'directory';
    action: 'tree';
}
interface FileUrl extends BaseUrl {
    kind: 'file';
    action: 'resolve' | 'blob';
    resolveUrl: string;
}
type HFUrl = DirectoryUrl | FileUrl;
export declare function getHuggingFaceSource(sourceId: string, options?: {
    requestInit?: RequestInit;
    accessToken?: string;
}): FileSource | DirSource | undefined;
export declare function parseHuggingFaceUrl(url: string): HFUrl;
/**
 * Parse Link HTTP header, eg `<https://huggingface.co/api/datasets/bigscience/P3/tree/main?recursive=1&cursor=...>; rel="next"`
 */
export declare function parseLinkHeader(header: string): Record<string, string>;
export {};
