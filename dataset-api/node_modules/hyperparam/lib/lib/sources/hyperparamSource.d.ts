import type { DirSource, FileSource } from './types.js';
export interface HyperparamFileMetadata {
    key: string;
    eTag?: string;
    fileSize?: number;
    lastModified: string;
}
export declare function getHyperparamSource(sourceId: string, { endpoint, requestInit }: {
    endpoint: string;
    requestInit?: RequestInit;
}): FileSource | DirSource | undefined;
