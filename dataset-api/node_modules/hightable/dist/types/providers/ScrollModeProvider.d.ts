import type { ReactNode } from 'react';
export interface ScrollModeProviderProps {
    padding?: number;
}
type Props = {
    children: ReactNode;
    headerHeight: number;
    numRows: number;
} & ScrollModeProviderProps;
export declare function ScrollModeProvider({ children, headerHeight, numRows, padding }: Props): import("react/jsx-runtime").JSX.Element;
export {};
