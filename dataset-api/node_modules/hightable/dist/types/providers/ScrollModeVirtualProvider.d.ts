import { type ReactNode } from 'react';
interface ScrollModeVirtualProviderProps {
    children: ReactNode;
    canvasHeight: number;
    headerHeight: number;
    numRows: number;
    padding: number;
}
export declare function ScrollModeVirtualProvider({ children, canvasHeight, headerHeight, numRows, padding }: ScrollModeVirtualProviderProps): import("react/jsx-runtime").JSX.Element;
export {};
