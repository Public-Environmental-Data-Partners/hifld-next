import type { ReactNode } from 'react';
interface ScrollModeNativeProviderProps {
    children: ReactNode;
    canvasHeight: number;
    numRows: number;
    padding: number;
}
export declare function ScrollModeNativeProvider({ children, canvasHeight, numRows, padding }: ScrollModeNativeProviderProps): import("react/jsx-runtime").JSX.Element;
export {};
