import type { CSSProperties, ReactNode } from 'react';
interface Props {
    checked?: boolean;
    children?: ReactNode;
    onCheckboxPress?: () => void;
    pendingSelectionGesture?: boolean;
    style?: CSSProperties;
    ariaColIndex: number;
    ariaRowIndex: number;
    setTableCornerSize?: (size: {
        width: number;
        height: number;
    }) => void;
}
export default function TableCorner({ children, checked, onCheckboxPress, pendingSelectionGesture, style, ariaColIndex, ariaRowIndex, setTableCornerSize }: Props): import("react/jsx-runtime").JSX.Element;
export {};
