import type { ColumnDescriptor, DataFrame, Obj } from './types.js';
interface ArrayData {
    readonly _array: Record<string, any>[];
    readonly _rowNumbers?: number[];
}
export declare function arrayDataFrame<M extends Obj, C extends Obj>(array: Record<string, any>[], rowNumbers?: number[], options?: {
    metadata?: M;
    columnDescriptors?: ColumnDescriptor<C>[];
}): ArrayData & DataFrame<M, C>;
export {};
