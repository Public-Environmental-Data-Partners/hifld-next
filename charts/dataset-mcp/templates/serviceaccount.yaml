{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "dataset-mcp.serviceAccountName" . }}
  labels: {{- include "dataset-mcp.labels" . | nindent 4 }}
{{- end }}
